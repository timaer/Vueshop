const moduleAddress={
    state: {
        choosedaddress:{}
    },
    mutations: {
        SET_ADDRESS(state,payload){
            state.choosedaddress=payload.address
        }
    },
    actions: {
        
    }
}

export default moduleAddress